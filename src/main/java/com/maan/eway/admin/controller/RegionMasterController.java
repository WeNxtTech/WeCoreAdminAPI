/*
*  Copyright (c) 2019. All right reserved
* Created on 2022-08-24 ( Date ISO 2022-08-24 - Time 12:58:26 )
* Generated by Telosys Tools Generator ( version 3.3.0 )
*/
package com.maan.eway.admin.controller;
import com.maan.eway.error.Error;
import com.maan.eway.admin.req.RegionMasterGetAllReq;
import com.maan.eway.admin.req.RegionMasterGetReq;
import com.maan.eway.admin.req.RegionMasterSaveReq;
import com.maan.eway.admin.res.RegionMasterRes;
import com.maan.eway.admin.service.RegionMasterService;
import com.maan.eway.bean.RegionMaster;
import com.maan.eway.res.CommonRes;
import com.maan.eway.res.DropDownRes;
import com.maan.eway.res.SuccessRes;
import com.maan.eway.service.PrintReqService;

import io.swagger.annotations.ApiOperation;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.Collections;
import java.util.List;


/**
* <h2>RegionMasterController</h2>
*/
@RestController
@RequestMapping("/master")
public class RegionMasterController {

	@Autowired
	private  RegionMasterService regionService;
	
	@Autowired
	private  PrintReqService reqPrinter;
	
	// save
		@PostMapping("/insertregion")
		@ApiOperation(value = "This method is Insert Region Details")
		public ResponseEntity<CommonRes> insertRegion(@RequestBody RegionMasterSaveReq req) {

			reqPrinter.reqPrint(req);
			CommonRes data = new CommonRes();

			List<Error> validation = regionService.validateRegionDetails(req);
			// validation
			if (validation != null && validation.size() != 0) {
				data.setCommonResponse(null);
				data.setIsError(true);
				data.setErrorMessage(validation);
				data.setMessage("Failed");
				return new ResponseEntity<CommonRes>(data, HttpStatus.OK);

			} else {

				// Get All
				SuccessRes res = regionService.insertRegion(req);
				data.setCommonResponse(res);
				data.setIsError(false);
				data.setErrorMessage(Collections.emptyList());
				data.setMessage("Success");

				if (res != null) {
					return new ResponseEntity<CommonRes>(data, HttpStatus.CREATED);
				} else {
					return new ResponseEntity<>(null, HttpStatus.BAD_REQUEST);
				}
			}

		}
		
		//  Get All Region Master
		
		@PostMapping("/getallregiondetails")
		@ApiOperation("This method is getall Region Details")
		public ResponseEntity<CommonRes> getallRegionDetails(@RequestBody RegionMasterGetAllReq req)
		{
			CommonRes data = new CommonRes();
			reqPrinter.reqPrint(req);
			
			List<RegionMasterRes> res = regionService.getallRegionDetails(req);
			data.setCommonResponse(res);
			data.setErrorMessage(Collections.emptyList());
			data.setIsError(false);
			data.setMessage("Success");
			
			if(res!= null) {
				return new ResponseEntity<CommonRes> (data, HttpStatus.CREATED);
			}
			else {
				return new ResponseEntity<> (null, HttpStatus.BAD_REQUEST);
			}
		}
		
	//  Get Active Region Master
		
			@PostMapping("/getactiveregion")
			@ApiOperation("This method is Active Region Details")
			public ResponseEntity<CommonRes> getActiveRegionDetails(@RequestBody RegionMasterGetAllReq req)
			{
				CommonRes data = new CommonRes();
				reqPrinter.reqPrint(req);
				
				List<RegionMasterRes> res = regionService.getActiveRegionDetails(req);
				data.setCommonResponse(res);
				data.setErrorMessage(Collections.emptyList());
				data.setIsError(false);
				data.setMessage("Success");
				
				if(res!= null) {
					return new ResponseEntity<CommonRes> (data, HttpStatus.CREATED);
				}
				else {
					return new ResponseEntity<> (null, HttpStatus.BAD_REQUEST);
				}
			}
		
		// Get By Region Id
		
		@PostMapping("/getbyregionid")
		@ApiOperation("This Method is to get by Region id")
		public ResponseEntity<CommonRes> getByRegionCode(@RequestBody RegionMasterGetReq req)
		{
		CommonRes data = new CommonRes();
		RegionMasterRes res = regionService.getByRegionCode(req);
		data.setCommonResponse(res);
		data.setErrorMessage(Collections.emptyList());
		data.setIsError(false);
		data.setMessage("Success");

		if (res != null) {
			return new ResponseEntity<CommonRes>(data, HttpStatus.CREATED);

		} else {
			return new ResponseEntity<>(null, HttpStatus.BAD_REQUEST);
		}
	}
		
		// Region Master Drop Down Type
		@GetMapping("/dropdown/region")
		@ApiOperation(value = "This method is get Region Master Drop Down")

		public ResponseEntity<CommonRes> getRegionMasterDropdown() {

			CommonRes data = new CommonRes();

			// Save
			List<DropDownRes> res = regionService.getRegionMasterDropdown();
			data.setCommonResponse(res);
			data.setIsError(false);
			data.setErrorMessage(Collections.emptyList());
			data.setMessage("Success");

			if (res != null) {
				return new ResponseEntity<CommonRes>(data, HttpStatus.CREATED);
			} else {
				return new ResponseEntity<>(null, HttpStatus.BAD_REQUEST);
			}

		}
		

}
