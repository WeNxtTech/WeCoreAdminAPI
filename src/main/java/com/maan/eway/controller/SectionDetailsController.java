/*
*  Copyright (c) 2019. All right reserved
* Created on 2022-09-06 ( Date ISO 2022-09-06 - Time 11:31:31 )
* Generated by Telosys Tools Generator ( version 3.3.0 )
*/
package com.maan.eway.controller;

import java.util.Collections;
import java.util.List;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.HttpStatus;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PostMapping;
import org.springframework.web.bind.annotation.RequestBody;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.maan.eway.bean.SectionDetails;
import com.maan.eway.error.Error;
import com.maan.eway.master.req.ProductsRiskSaveReq;
import com.maan.eway.master.req.RiskDomesticDetailsSaveReq;
import com.maan.eway.res.CommonRes;
import com.maan.eway.res.SuccessRes;
import com.maan.eway.service.PrintReqService;
import com.maan.eway.service.SectionDetailsService;
import com.maan.eway.service.ValidationService;

import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;


/**
* <h2>SectionDetailsController</h2>
*/
@RestController
@Api(tags = "SECTION : Details ", description = "API's")
@RequestMapping("/api")
public class SectionDetailsController {

	@Autowired
	private PrintReqService reqPrinter;
	
	@Autowired
	private  ValidationService validationService;
	
	@Autowired
	private  SectionDetailsService entityService;

	@PostMapping("/saveproductsections")
	@ApiOperation(value = "This method is Insert Section Details")
	public ResponseEntity<CommonRes> saveProductSectionDetails(@RequestBody ProductsRiskSaveReq req) {

		reqPrinter.reqPrint(req);
		CommonRes data = new CommonRes();

		List<Error> validation = validationService.validateProductSections(req);
		// validation
		if (validation != null && validation.size() != 0) {
			data.setCommonResponse(null);
			data.setIsError(true);
			data.setErrorMessage(validation);
			data.setMessage("Failed");
			return new ResponseEntity<CommonRes>(data, HttpStatus.OK);

		} else {
			// Save
			SuccessRes res = entityService.saveProductSections(req);
			data.setCommonResponse(res);
			data.setIsError(false);
			data.setErrorMessage(Collections.emptyList());
			data.setMessage("Success");

			if (res != null) {
				return new ResponseEntity<CommonRes>(data, HttpStatus.CREATED);
			} else {
				return new ResponseEntity<>(null, HttpStatus.BAD_REQUEST);
			}
		}

	}
/*
	private static final String ENTITY_TITLE = "SectionDetails";


 	public SectionDetailsController (SectionDetailsService entityService) {
		this.entityService = entityService;
	}
*/

	@PostMapping(value = "/sectiondetails")
	public ResponseEntity<SectionDetails> createSectionDetails(@RequestBody  SectionDetails model) {

   		 SectionDetails data = entityService.create(model);
    		if (data != null) {
    			return new ResponseEntity<>(data,HttpStatus.CREATED);
  			  } else {
    			return new ResponseEntity<>(null,HttpStatus.BAD_REQUEST);
   			 }
    }

    @GetMapping(value = "/sectiondetails")
    public ResponseEntity<List<SectionDetails>> getAllSectionDetails() {
        List<SectionDetails> lst = entityService.getAll();

        return new ResponseEntity<>(lst,HttpStatus.OK);
    }
/*
        @GetMapping(value = "/sectiondetails/{id}")
    public ResponseEntity<SectionDetails> getOneSectionDetails(@PathVariable("id") long id) {

            SectionDetails e = entityService.getOne(id);
            if (e == null) {
            	return new ResponseEntity<>(null,HttpStatus.NOT_FOUND);
            }
            return new ResponseEntity<>(e, HttpStatus.OK);
    }

*/

}
