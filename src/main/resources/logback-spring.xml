<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console logging -->
    <appender name="CONSOLE"
              class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>
                %d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
            </pattern>
        </encoder>
    </appender>

    <!-- Loki logging -->
    <appender name="LOKI"
              class="com.github.loki4j.logback.Loki4jAppender">

        <http>
            <url>http://65.0.113.151:3100/loki/api/v1/push</url>
        </http>

        <format>

            <label>
                <pattern>app=WeCore-ADMIN,level=%level</pattern>
            </label>

            <message>
                <pattern>%msg</pattern>
            </message>

        </format>

    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="LOKI"/>
    </root>

</configuration>